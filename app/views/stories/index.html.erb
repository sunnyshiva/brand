<h1 class="current-category"><%= params[:category] %></h1>

<% if @stories.count == 0 %>
	<h3> There are no stories in this category </h3>
<% else %>
	<% @stories.each do |story| %>
		<h2><%= link_to story.title, story_path(story) %></h2>
	<% end %>
<% end %>